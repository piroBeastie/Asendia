# ══════════════════════════════════════════════════════════════════════
#  SECRETS ONLY. Copy this to .env and fill in the values.
#  Model / voice choices live in config.py, not here.
# ══════════════════════════════════════════════════════════════════════

# Gemini — powers BOTH the conversation AND the scoring (free tier, one key).
# https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key

# Supabase — stores the scored report (table: interviews)
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_key
